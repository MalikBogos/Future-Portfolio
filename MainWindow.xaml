<Window x:Class="FuturePortfolio.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:FuturePortfolio"
            mc:Ignorable="d"
            Title="DoWell" Height="450" Width="800">
    <Window.Resources>
        <local:CellTemplateSelector x:Key="CellTemplateSelector">
            <local:CellTemplateSelector.DefaultTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Value}"
              FontStyle="{Binding Format.FontStyle, UpdateSourceTrigger=PropertyChanged}"
              FontWeight="{Binding Format.FontWeight, UpdateSourceTrigger=PropertyChanged}"
              Foreground="{Binding Format.ForegroundColor, UpdateSourceTrigger=PropertyChanged}"
              Background="{Binding Format.BackgroundColor, UpdateSourceTrigger=PropertyChanged}"
              Padding="5,2"/>
                </DataTemplate>
            </local:CellTemplateSelector.DefaultTemplate>
            <local:CellTemplateSelector.EditingTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Value}"
              FontStyle="{Binding Format.FontStyle, UpdateSourceTrigger=PropertyChanged}"
              FontWeight="{Binding Format.FontWeight, UpdateSourceTrigger=PropertyChanged}"
              Foreground="{Binding Format.ForegroundColor, UpdateSourceTrigger=PropertyChanged}"
              Background="{Binding Format.BackgroundColor, UpdateSourceTrigger=PropertyChanged}"
              Padding="5,2"/>
                </DataTemplate>
            </local:CellTemplateSelector.EditingTemplate>
        </local:CellTemplateSelector>

        <DataTemplate x:Key="CellTemplate">
            <ContentPresenter Content="{Binding}"
                              ContentTemplateSelector="{StaticResource CellTemplateSelector}"/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">

            <ToggleButton x:Name="BoldButton" Click="BoldButton_Click" Padding="5,2" ToolTip="Bold">
                <TextBlock Text="B" FontWeight="Bold"/>
            </ToggleButton>
            <ToggleButton x:Name="ItalicButton" Click="ItalicButton_Click" Padding="5,2" ToolTip="Italic">
                <TextBlock Text="I" FontStyle="Italic"/>
            </ToggleButton>
            
            <Separator/>

            <Button x:Name="TextColorButton" Click="TextColorButton_Click" Padding="5,2" ToolTip="Text Color">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="A" Margin="0,0,5,0"/>
                    <Rectangle Width="16" Height="16" Stroke="Black" StrokeThickness="1">
                        <Rectangle.Fill>
                            <SolidColorBrush x:Name="TextColorIndicator" Color="Black"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </StackPanel>
            </Button>

            <Button x:Name="BackgroundColorButton" Click="BackgroundColorButton_Click" Padding="5,2" ToolTip="Cell Color">
                <Rectangle Width="16" Height="16" Stroke="Black" StrokeThickness="1">
                    <Rectangle.Fill>
                        <SolidColorBrush x:Name="BackgroundColorIndicator" Color="White"/>
                    </Rectangle.Fill>
                </Rectangle>
            </Button>

            <Button x:Name="AddRowButton" Content="Add Row" Click="AddRowButton_Click" Padding="5,2"/>
            <Button x:Name="AddColumnButton" Content="Add Column" Click="AddColumnButton_Click" Padding="5,2"/>
            <Button x:Name="RemoveRowButton" Content="Remove Row" Click="RemoveRowButton_Click" Padding="5,2"/>
            <Button x:Name="RemoveColumnButton" Content="Remove Column" Click="RemoveColumnButton_Click" Padding="5,2"/>
        </ToolBar>
        <DataGrid x:Name="ExcelLikeGrid" Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      CellEditEnding="ExcelLikeGrid_CellEditEnding"
                      SelectionChanged="ExcelLikeGrid_SelectionChanged"
                      HeadersVisibility="All"
                      SelectionUnit="Cell"
                      SelectionMode="Single"
                      RowHeaderWidth="25"
                      GridLinesVisibility="None"
                      HorizontalGridLinesBrush="LightGray"
                      VerticalGridLinesBrush="LightGray">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF" Opacity="0.4"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#3399FF" Opacity="0.4"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>
            </DataGrid.Resources>
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border BorderThickness="{TemplateBinding BorderThickness}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            Background="{TemplateBinding Background}">
                                    <ContentPresenter Margin="1"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#3399FF20"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <Setter Property="Background" Value="#F0F0F0"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
    </Grid>
</Window>